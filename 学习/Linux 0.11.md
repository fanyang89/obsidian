# 开机

当你按下开机键的那一刻，在主板上提前写死的固件程序 BIOS 会将硬盘中启动区的 512 字节的数据，原封不动复制到内存中的 0x7c00 位置处，并跳转到这里运行。
![](assets/Pasted%20image%2020230622232551.png)

Intel 手册规定，开机后 PC 寄存器要初始化为 0xFFFF0，也就是从这个内存地址开始，执行 CPU 的第一条指令。
CPU 地址线连接的有 RAM（也就是我们常说的内存），有 ROM（也就是我们上图中的 BIOS），还有一些外设的 IO 端口，叫做 Memory-Mapped IO
