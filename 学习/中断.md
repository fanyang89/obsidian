# 中断

中断可以由硬件产生（称为外部中断），也可以由软件产生（称为内部中断），在程序中写入 `int n` 指令可以产生 `n` 号中断和异常（`n` 从 `0-0xff` ）。早期的操作系统甚至是通过中断来进行内核调用的。

`int` 指令是一种从 ring3 进入 ring 0 的方法。比如 Windows 在 XP 版本之前使用的 `int 2e`。在 x86 CPU 提供了 `sysenter` 指令后，这种方式才被放弃。
