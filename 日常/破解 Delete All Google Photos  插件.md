---
title: 破解 Delete All Google Photos 插件
---

# 破解 Delete All Google Photos 插件

#google #hack

[Delete All Google Photos](https://chromewebstore.google.com/detail/delete-all-google-photos/bebhhjmapjadpdkkhbkpnpbjhkhndofl)

扩展 ID：`bebhhjmapjadpdkkhbkpnpbjhkhndofl`

试用版每天只能删 500 张

```js
const R = 1e3,
  M = 1000000;
function E(i) {
  return new Promise((e) => setTimeout(e, i));
}
```

`M` 改成很大的值。

```js
if (
  !!P &&
  (console.log("removing undo delete button and continuing", new Date()),
  P.remove(),
  (r += b),
  localStorage.setItem("num_deleted", 0),
  localStorage.setItem("last_deleted_at", new Date().getTime()),
  !(o != null && o.paid) && r >= M)
) {
  t(0, (p = !0));
  return;
}
```

每次 `r += b` 然后设置 `num_deleted`。改成每次 `num_deleted` 都设置为 `0`

然后开发人员模式加载扩展
