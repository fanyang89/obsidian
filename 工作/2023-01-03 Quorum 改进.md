着眼点：

1. Leader 如何得知自己已经失效？

# 通常做法

- Meta 跟 zk 断开连接时，就立即让自己失效
  - 问题：当网络不稳定时，比如断开后立即连接成功

# 改进计划

1. zk.cc 加入异步支持
2. zk.cc 加入 request timeout 支持
3. 修改 CheckZK() 周期为 1s
4. 移除 local session timeout
   - 由 CheckZK 触发 session expired
5. 修改 zk connection timeout
6.
