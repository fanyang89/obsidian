- ACreate
- ACreateSequential
- AWatch
- AWatchChildren
- AExists
- AGet
- ASet
- AMove
- AGetChildren
- AGetSize
- AGetChildrenAndSize
- ADelete
- ADeleteChildrenAndSelf
- AMultiDeleteChildrenAndSelf

ACL TODO

- 修改 skipACL=yes -> no
- 初始化 zk client 时自动检测 ACL，如果开启则自动认证
- 增加 ACL digest 计算。命令行和 ZBS 内部都支持
- 支持 ACL get/set
- 移除了 local session timeout
- 修复了 zk client 一直存在的内存泄漏
- 增加 async api 中的 backoff
- WIP 增加 zoo api mock
- WIP 如果 zk 开启了 ACL，但是 ZBS 没有开启，则设置 ACL
- WIP 增加 RPC 开启 ACL

```c++
DEFINE_string(meta_zk_config_path,
              gflags::StringFromEnv("META_ZK_CONFIG_PATH",
                                    "/smx/cfg/meta"),
              "the meta cluster config path on the zookeeper.");
DEFINE_string(zk_journal_path,
              gflags::StringFromEnv("META_ZK_JOURNAL_PATH",
                                    "/zbs/meta/__mj"),
              "the journal path on the zookeeper.");
DEFINE_string(taskd_zk_db_path,
              gflags::StringFromEnv("TASKD_ZK_DB_PATH",
                                    "/zbs/taskd/db"),
              "the taskd zk db path");
```
